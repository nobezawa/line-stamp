<%= form_for(@project_image) do |f| %>
  <% if @project_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_image.errors.count, "error") %> prohibited this project_image from being saved:</h2>

      <ul>
      <% @project_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :project_id %><br>
    <%= f.number_field :project_id %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
